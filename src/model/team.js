export class Team {
    constructor(code, name, short_name, pulse_id, id) {
        this.code = code;
        this.id = id;
        this.name = name;
        this.short_name = short_name;
        this.pulse_id = pulse_id;
    }
}